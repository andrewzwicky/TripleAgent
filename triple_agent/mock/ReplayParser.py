# this class only exists to allow tools/IDEs to parse the
# code without throwing a bunch of errors.  It's never actually
# run.
class ReplayParser:  # pragma: no cover
    def __init__(self, _):
        raise NotImplementedError
